import { StyleSheet } from 'react-native'
import ui from '../../constants/css'

export default StyleSheet.create({
	recipe: {},
	recipe__note: {
		textAlign: 'center',
		color: 'black',
		fontSize: ui.fontM,
		margin: 20
	}
})
