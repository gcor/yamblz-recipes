import React from 'react'
import {
	View,
	Text,
	TouchableOpacity
} from 'react-native'
import s from './SetTimerButton.css'

const SetTimerButton = () => (
	<TouchableOpacity>
		<Text>Поставить таймер</Text>
	</TouchableOpacity>
)

export default SetTimerButton
